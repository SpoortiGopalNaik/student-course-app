<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title><%= title || 'Student Course App' %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <style>
    body { font-family: Arial, Helvetica, sans-serif; margin: 0; padding: 0; }
    header { background:#007bff; color: #fff; padding: 12px 20px; }
    .container { padding: 20px; max-width: 900px; margin: 0 auto; }
    nav a { color: #fff; margin-right: 12px; text-decoration: none; }
    .small { font-size: 0.9rem; color:#666; }
    .btn { padding:8px 12px; border: none; cursor: pointer; border-radius:4px; }
    .btn-primary { background:#007bff; color:#fff; }
  </style>
</head>
<body>
  <header>
    <nav>
      <a href="/">Home</a>
      <% if (currentUser) { %>
        <span style="margin-left:10px">Welcome, <strong><%= currentUser.name %></strong></span>
        <a href="/courses" style="margin-left:20px">Courses</a>
        <a href="/logout" style="margin-left:12px">Logout</a>
      <% } else { %>
        <a href="/login">Login</a>
        <a href="/register" style="margin-left:12px">Register</a>
      <% } %>
    </nav>
  </header>

  <div class="container">
    <%- body %>
  </div>
</body>
</html>
